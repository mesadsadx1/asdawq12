# 🌙 Dream Interpreter Pro

AI-powered dream interpretation app with modern React UI

## 🚀 Quick Start

1. Clone the repository
2. Install dependencies:
   ```bash
   # Server
   cd server && npm install
   
   # Client  
   cd client && npm install

   Для полной работы приложения нужна ollama

   устанавливаете ollama с оффициального сайта

   после этого вводите в консоль

   ollama pull mistral

   ollama serve (если выдает ошибку перезагружаем ПК)

   после этого заходим в проект в главную часть (гле client и server) 
   
   открываем консоль в каждой из этих папок и прописываем поочередно для client и для server (не закрывая консоли)

   npm install && npm start


   
"# asdawq12" 
